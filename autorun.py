import os
os.system("make") #runs make on the system(might not work if using windows)
os.system("java -cp ./bin clubSimulation.ClubSimulation 20 5 5 5") #runs the code with a specific set of parameters
